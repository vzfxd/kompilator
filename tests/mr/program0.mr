# prosta translacja z użyciem pamięci
# n -> P0, p -> P1
READ                   wczytanie zmiennej do rejestru a
RST b                  reset rejestru b
STORE b                wczytanie zmiennej do komórki val(r_b), czyli p0
RST b                  reset rejestru b
LOAD b                 wczytanie komórki val(r_b) do rejestru a
SHR a                  dzielimy / 2 rejestr a
SHL a                  mnozymy * 2 rejestr a

     
RST b                   
INC b
STORE b
RST b
LOAD b
PUT c
RST b
INC b
LOAD b
PUT d
GET c
SUB d
JZERO 24
RST a
INC a
WRITE
JUMP 26
RST a
WRITE
RST b
LOAD b
SHR a
RST b
STORE b
RST b
LOAD b
JPOS 3
HALT
